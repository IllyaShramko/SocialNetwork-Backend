model User {
  id        Int       @id @default(autoincrement())
  email     String    @unique
  username  String?   @unique
  firstName String?
  surname   String?
  avatarUrl String?
  birthday  DateTime?
  signature String?
  password  String
  isActive  Boolean   @default(false)
}
