model Album {
  id      Int      @id @default(autoincrement())
  name    String
  topic   Tag?     @relation(fields: [topicId], references: [id])
  topicId Int?
  date    DateTime
  images  Image[]
}
